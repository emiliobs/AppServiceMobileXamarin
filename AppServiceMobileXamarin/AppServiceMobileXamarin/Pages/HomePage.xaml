<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppServiceMobileXamarin.Pages.HomePage" Title="Services.">

    <ScrollView>
        <StackLayout Spacing="10" Padding="1" VerticalOptions="Center">
            <Button x:Name="productsButton"
                    Text="Products."
                    BackgroundColor="Yellow"
                    TextColor="Black"
                    HorizontalOptions="FillAndExpand"
                   VerticalOptions="Center">
            </Button>
            <Button x:Name="serviciesButton"
                    Text="Services."
                    BackgroundColor="Aqua"
                    TextColor="Black"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="Center">
            </Button>
            <Button x:Name="queriesButton"
                    Text="Queries."
                    BackgroundColor="Purple"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="Center">
            </Button>
        </StackLayout>
    </ScrollView>

</ContentPage>